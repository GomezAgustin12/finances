{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "bootstrap": "yarn install -W && lerna bootstrap"
  },
  "devDependencies": {
    "lerna": "^6.6.1"
  }
}
